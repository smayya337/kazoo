# Wazuh Ansible Setup

Before using this, edit `files/config.yml` and run `setup.sh`. Then go into `hosts.ini` and edit the files, setting the host names as their IP addresses and the `node_id` variable as their node IDs.

This repository installs Wazuh 4.7.0.
